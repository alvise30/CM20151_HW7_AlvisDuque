tmp <- strptime(sdate[i], format="%Y.%m.%d_%H:%M:%S")-strptime(sdate[i-1], format="%Y.%m.%d_%H:%M:%S")
if (tmp!=45){
tmp = tmp*60.
x <- c(x,tmp)
} else {
x <- c(x,tmp)
}
}
#Leer el archivo
times <- read.csv('times.csv')
#Cargar los datos
date <- times[,2]
#Creamos un vector para guardar las diferencias de tiempos obtenidas
x <- c(NULL)
tmp <- 0
#Calculamos todas las diferencias de tiempo
for (i in 2:length(date)) {
tmp <- 0
tmp <- strptime(sdate[i], format="%Y.%m.%d_%H:%M:%S")-strptime(sdate[i-1], format="%Y.%m.%d_%H:%M:%S")
if (tmp!=45){
tmp = tmp*60.
x <- c(x,tmp)
} else {
x <- c(x,tmp)
}
}
#Leer el archivo
times <- read.csv('times.csv')
#Cargar los datos
date <- times[,2]
#Creamos un vector para guardar las diferencias de tiempos obtenidas
x <- c(NULL)
tmp <- 0
#Calculamos todas las diferencias de tiempo
for (i in 2:length(date)) {
tmp <- 0
tmp <- strptime(date[i], format="%Y.%m.%d_%H:!%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S")
if (tmp!=45){
tmp = tmp*60.
x <- c(x,tmp)
} else {
x <- c(x,tmp)
}
}
#Leer el archivo
times <- read.csv('times.csv')
#Cargar los datos
date <- times[,2]
#Creamos un vector para guardar las diferencias de tiempos obtenidas
x <- c(NULL)
tmp <- 0
#Calculamos todas las diferencias de tiempo
for (i in 2:length(date)) {
tmp <- 0
tmp <- (strptime(date[i], format="%Y.%m.%d_%H:!%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S"))
if (tmp!=45){
tmp = tmp*60.
x <- c(x,tmp)
} else {
x <- c(x,tmp)
}
}
#Calculamos todas las diferencias de tiempo
for (i in 2:length(date)) {
tmp <- 0
tmp <- (strptime(date[i], format="%Y.%m.%d_%H:!%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S"))
if (tmp==45){
tmp = tmp*60.
x <- c(x,tmp)
} else {
x <- c(x,tmp)
}
}
for (i in 2:length(date)) {
tmp <- 0
tmp <- (strptime(date[i], format="%Y.%m.%d_%H:!%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S"))
tmp = (strptime(date[i], format="%Y.%m.%d_%H:!%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S"))
tmp <-c(NULL)
tmp = c(strptime(date[i], format="%Y.%m.%d_%H:!%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S"))
if (tmp==45){
tmp = tmp*60.
x <- c(x,tmp)
} else {
x <- c(x,tmp)
}
}
#Calculamos todas las diferencias de tiempo
for (i in 2:length(date)) {
tmp <-c(NULL)
tmp = c(strptime(date[i], format="%Y.%m.%d_%H:!%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S"))
if (tmp==45){
tmp = tmp*60.
x <- c(x,tmp)
} else {
x <- c(x,tmp)
}
}
#Leer el archivo
times <- read.csv('times.csv')
#Cargar los datos
date <- times[,2]
#Creamos un vector para guardar las diferencias de tiempos obtenidas
x <- c(NULL)
tmp <- 0
#Calculamos todas las diferencias de tiempo
for (i in 2:length(date)) {
tmp <-c(NULL)
tmp <- c(tmp,strptime(date[i], format="%Y.%m.%d_%H:!%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S"))
if (tmp==45){
tmp = tmp*60.
x <- c(x,tmp)
} else {
x <- c(x,tmp)
}
}
for (i in 2:length(date)) {
tmp <-c(NULL)
tmp <- c(tmp,strptime(date[i], format="%Y.%m.%d_%H:!%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S"))
if (tmp==45){
tmp = tmp*60.
x <- c(x,tmp)
} else {
x <- c(x,tmp)
}
}
View(x)
#Leer el archivo
times <- read.csv('times.csv')
#Cargar los datos
date <- times[,2]
#Creamos un vector para guardar las diferencias de tiempos obtenidas
x <- c(NULL)
tmp <- 0
#Calculamos todas las diferencias de tiempo
for (i in 2:length(date)) {
#tmp <-c(NULL)
#tmp <- c(tmp,strptime(date[i], format="%Y.%m.%d_%H:!%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S"))
if ((strptime(date[i], format="%Y.%m.%d_%H:!%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S"))==45){
x <- c(x,strptime(date[i], format="%Y.%m.%d_%H:!%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S"))
} else {
x <- c(x,strptime(date[i], format="%Y.%m.%d_%H:!%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S"))
}
}
for (i in 2:length(date)) {
x <- c(x,strptime(date[i], format="%Y.%m.%d_%H:!%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S"))
}
#Creamos un vector para guardar las diferencias de tiempos obtenidas
x <- c(NULL)
tmp <- 0
#Calculamos todas las diferencias de tiempo
for (i in 2:length(date)) {
x <- c(x,strptime(date[i], format="%Y.%m.%d_%H:!%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S"))
}
#Leer el archivo
times <- read.csv('times.csv')
#Cargar los datos
date <- times[,2]
#Creamos un vector para guardar las diferencias de tiempos obtenidas
x <- c(NULL)
tmp <- 0
#Calculamos todas las diferencias de tiempo
for (i in 2:length(date)) {
x <- c(x,strptime(date[i], format="%Y.%m.%d_%H:!%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S"))
}
#Leer el archivo
times <- read.csv('times.csv')
#Cargar los datos
date <- times[,2]
#Creamos un vector para guardar las diferencias de tiempos obtenidas
x <- c(NULL)
tmp <- 0
#Calculamos todas las diferencias de tiempo
for (i in 2:length(date)) {
x <- c(x,strptime(date[i], format="%Y.%m.%d_%H:!%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S"))
}
#Leer el archivo
times <- read.csv('times.csv')
#Cargar los datos
date <- times[,2]
#Creamos un vector para guardar las diferencias de tiempos obtenidas
x <- c(NULL)
tmp <- 0
#Calculamos todas las diferencias de tiempo
for (i in 2:length(date)) {
x <- c(x,strptime(date[i], format="%Y.%m.%d_%H:!%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S"))
}
#Leer el archivo
times <- read.csv('times.csv')
#Cargar los datos
date <- times[,2]
#Creamos un vector para guardar las diferencias de tiempos obtenidas
x <- c(NULL)
tmp <- 0
#Calculamos todas las diferencias de tiempo
for (i in 2:length(date)) {
x <- c(x, (strptime(date[i], format="%Y.%m.%d_%H:!%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S")))
}
#Leer el archivo
times <- read.csv('times.csv')
#Cargar los datos
date <- times[,2]
#Creamos un vector para guardar las diferencias de tiempos obtenidas
x <- c(NULL)
tmp <- 0
#Calculamos todas las diferencias de tiempo
for (i in 2:length(date)) {
x <- c(x, (strptime(date[i], format="%Y.%m.%d_%H:!%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S")))
}
x <- c(strptime(date[i], format="%Y.%m.%d_%H:!%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S"))
#Creamos un vector para guardar las diferencias de tiempos obtenidas
x <- c(NULL)
tmp <- 0
#Calculamos todas las diferencias de tiempo
for (i in 2:length(date)) {
x <- c(strptime(date[i], format="%Y.%m.%d_%H:!%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S"))
}
View(date)
#Leer el archivo
times <- read.csv('times.csv')
#Cargar los datos
date <- times[,2]
#Creamos un vector para guardar las diferencias de tiempos obtenidas
x <- c(NULL)
#Calculamos todas las diferencias de tiempo
for (i in 2:20) {
x <- c(x,strptime(sdate[i], format="%Y.%m.%d_%H:%M:%S")-strptime(sdate[i-1], format="%Y.%m.%d_%H:%M:%S"))
}
#Leer el archivo
x <- c(NULL)
#Calculamos todas las diferencias de tiempo
for (i in 2:20) {
x <- c(x,strptime(date[i], format="%Y.%m.%d_%H:%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S"))
}
#Leer el archivo
times <- read.csv('times.csv')
#Cargar los datos
date <- times[,2]
#Creamos un vector para guardar las diferencias de tiempos obtenidas
x <- c(NULL)
#Calculamos todas las diferencias de tiempo
for (i in 2:20) {
tmp <- strptime(date[i], format="%Y.%m.%d_%H:%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S")
x <- c(x,tmp)
}
#Leer el archivo
times <- read.csv('times.csv')
#Cargar los datos
date <- times[,2]
#Creamos un vector para guardar las diferencias de tiempos obtenidas
x <- c(NULL)
#Calculamos todas las diferencias de tiempo
for (i in 2:20) {
tmp <- strptime(date[i], format="%Y.%m.%d_%H:%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S")
x <- c(x,tmp)
}
#Calculamos todas las diferencias de tiempo
for (i in 2:20) {
tmp <- strptime(date[i], format="%Y.%m.%d_%H:%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S")
x <- c(x,tmp)
write.txt(x[i-1], file = 'times.txt')
}
#Calculamos todas las diferencias de tiempo
for (i in 2:20) {
tmp <- strptime(date[i], format="%Y.%m.%d_%H:%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S")
x <- c(x,tmp)
fileConn<-file("output.txt")
writeLines(x[i-1], fileConn)
close(fileConn)
}
for (i in 2:20) {
tmp <- strptime(date[i], format="%Y.%m.%d_%H:%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S")
x <- c(x,tmp)
fileConn<-file("output.txt")
writeLines(x, fileConn)
close(fileConn)
}
#Calculamos todas las diferencias de tiempo
for (i in 2:length(dates)) {
tmp <- strptime(date[i], format="%Y.%m.%d_%H:%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S")
x <- c(x,tmp)
}
#Creamos un vector para guardar las diferencias de tiempos obtenidas
x <- c(NULL)
#Calculamos todas las diferencias de tiempo
for (i in 2:length(date)) {
tmp <- strptime(date[i], format="%Y.%m.%d_%H:%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S")
x <- c(x,tmp)
}
fileConn<-file("output.txt")
writeLines(x, fileConn)
close(fileConn)
fileConn<-file("output.txt")
write(x, fileConn)
close(fileConn)
#Leer el archivo
times <- read.csv('times.csv')
#Cargar los datos
date <- times[,2]
#Creamos un vector para guardar las diferencias de tiempos obtenidas
x <- c(NULL)
#Calculamos todas las diferencias de tiempo
for (i in 2:length(date)) {
tmp <- strptime(date[i], format="%Y.%m.%d_%H:%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S")
x <- c(x,tmp)
fileConn<-file("output1.txt")
write(x[i-1], fileConn)
close(fileConn)
}
#Leer el archivo
times <- read.csv('times.csv')
#Cargar los datos
date <- times[,2]
#Creamos un vector para guardar las diferencias de tiempos obtenidas
x <- c(NULL)
fileConn<-file("output1.txt")
#Calculamos todas las diferencias de tiempo
for (i in 2:length(date)) {
tmp <- strptime(date[i], format="%Y.%m.%d_%H:%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S")
x <- c(x,tmp)
write(x[i-1], fileConn)
}
close(fileConn)
x <- c(NULL)
fileConn<-file("output2.txt")
#Calculamos todas las diferencias de tiempo
for (i in 2:length(date)) {
tmp <- strptime(date[i], format="%Y.%m.%d_%H:%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S")
x <- c(x,tmp)
write(x[i-1], fileConn)
}
close(fileConn)
#Creamos un vector para guardar las diferencias de tiempos obtenidas
x <- c(NULL)
fileConn<-file("output2.txt")
#Calculamos todas las diferencias de tiempo
for (i in 2:length(date)) {
tmp <- strptime(date[i], format="%Y.%m.%d_%H:%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S")
x <- c(x,tmp)
write(x[i-1], fileConn, append = T)
}
close(fileConn)
#Leer el archivo
times <- read.csv('times.csv')
#Cargar los datos
date <- times[,2]
#Creamos un vector para guardar las diferencias de tiempos obtenidas
x <- c(NULL)
fileConn<-file("output2.txt")
#Calculamos todas las diferencias de tiempo
for (i in 2:length(date)) {
tmp <- strptime(date[i], format="%Y.%m.%d_%H:%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S")
x <- c(x,tmp)
write(x[i-1], fileConn, append = T)
}
close(fileConn)
#Creamos un vector para guardar las diferencias de tiempos obtenidas
x <- c(NULL)
fileConn<-file("output2.txt")
#Calculamos todas las diferencias de tiempo
for (i in 2:length(date)) {
tmp <- strptime(date[i], format="%Y.%m.%d_%H:%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S")
x <- c(x,tmp)
write(x[i-1], fileConn, append = TRUE, sep = '\n')
}
close(fileConn)
fileConn<-file("output2.txt")
write(x, fileConn, append = TRUE, sep = '\n')
close(fileConn)
#Leer el archivo
times <- read.csv('times.csv')
#Cargar los datos
date <- times[,2]
#Creamos un vector para guardar las diferencias de tiempos obtenidas
x <- c(NULL)
#Calculamos todas las diferencias de tiempo
for (i in 2:length(date)) {
tmp <- strptime(date[i], format="%Y.%m.%d_%H:%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S")
x <- c(x,tmp)
}
#Escribimos un .txt para guardar los datos
fout<-file("output2.txt")
write(x, fout, sep = '\n')
close(fout)
#Leer el archivo
times <- read.csv('times.csv')
#Cargar los datos
date <- times[,2]
#Creamos un vector para guardar las diferencias de tiempos obtenidas
x <- c(NULL)
#Calculamos todas las diferencias de tiempo
for (i in 2:length(date)) {
tmp <- strptime(date[i], format="%Y.%m.%d_%H:%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S")
x <- c(x,tmp)
}
#Escribimos un .txt para guardar los datos
fout<-file("intervalos.txt")
write(x, fout, sep = '\n')
close(fout)
source('~/.active-rstudio-document', echo=TRUE)
#Leer el archivo
times <- read.csv('times.csv')
#Cargar los datos
date <- times[,2]
#Creamos un vector para guardar las diferencias de tiempos obtenidas
x <- c(NULL)
#Calculamos todas las diferencias de tiempo
for (i in 2:length(date)) {
tmp <- strptime(date[i], format="%Y.%m.%d_%H:%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S")
x <- c(x,tmp)
}
#Escribimos un .txt para guardar los datos
fout<-file("intervalos.txt")
write(x, fout, sep = '\n')
close(fout)
library(lubridate)
#Leer el archivo
times <- read.csv('times.csv')
#Cargar los datos
date <- times[,2]
#Creamos un vector para guardar las diferencias de tiempos obtenidas
x <- c(NULL)
#Calculamos todas las diferencias de tiempo
for (i in 2:length(date)) {
tmp <- strptime(date[i], format="%Y.%m.%d_%H:%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S")
x <- c(x,tmp)
}
#Escribimos un .txt para guardar los datos
fout<-file("intervalos.txt")
write(x, fout, sep = '\n')
close(fout)
library(lubridate)
#Leer el archivo
times <- read.csv('times.csv')
#Cargar los datos
date <- times[,2]
#Creamos un vector para guardar las diferencias de tiempos obtenidas
x <- c('Intervalo')
#Calculamos todas las diferencias de tiempo
for (i in 2:length(date)) {
tmp <- strptime(date[i], format="%Y.%m.%d_%H:%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S")
x <- c(x,tmp)
}
#Escribimos un .txt para guardar los datos
fout<-file("intervalos.txt")
write(x, fout, sep = '\n')
close(fout)
library(lubridate)
#Leer el archivo
times <- read.csv('times.csv')
#Cargar los datos
date <- times[,2]
#Creamos un vector para guardar las diferencias de tiempos obtenidas
x <- c('Intervalo')
#Calculamos todas las diferencias de tiempo
for (i in 2:(length(date)+1)) {
tmp <- strptime(date[i], format="%Y.%m.%d_%H:%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S")
x <- c(x,tmp)
}
#Escribimos un .txt para guardar los datos
fout<-file("intervalos.txt")
write(x, fout, sep = '\n')
close(fout)
library(lubridate)
#Leer el archivo
times <- read.csv('times.csv')
#Cargar los datos
date <- times[,2]
#Creamos un vector para guardar las diferencias de tiempos obtenidas
x <- c('Intervalo')
#Calculamos todas las diferencias de tiempo
for (i in 2:length(date)) {
tmp <- strptime(date[i], format="%Y.%m.%d_%H:%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S")
x <- c(x,tmp)
}
#Escribimos un .txt para guardar los datos
fout<-file("intervalos.txt")
write(x, fout, sep = '\n')
close(fout)
library(lubridate)
#Leer el archivo
times <- read.csv('times.csv')
#Cargar los datos
date <- times[,2]
#Creamos un vector para guardar las diferencias de tiempos obtenidas
x <- c('Intervalo',45.0)
#Calculamos todas las diferencias de tiempo
for (i in 2:length(date)) {
tmp <- strptime(date[i], format="%Y.%m.%d_%H:%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S")
x <- c(x,tmp)
}
x <- c(x,45.0)
#Escribimos un .txt para guardar los datos
fout<-file("intervalos.txt")
write(x, fout, sep = '\n')
close(fout)
library(lubridate)
#Leer el archivo
times <- read.csv('times.csv')
#Cargar los datos
date <- times[,2]
#Creamos un vector para guardar las diferencias de tiempos obtenidas
x <- c('Intervalo',45.0)
#Calculamos todas las diferencias de tiempo
for (i in 2:length(date)) {
tmp <- strptime(date[i], format="%Y.%m.%d_%H:%M:%S")-strptime(date[i-1], format="%Y.%m.%d_%H:%M:%S")
x <- c(x,tmp)
}
#x <- c(x,45.0)
#Escribimos un .txt para guardar los datos
fout<-file("intervalos.txt")
write(x, fout, sep = '\n')
close(fout)
